import express from 'express';

const mainRouter = express();

export class Router {
    constructor() {}

    // funciones para declarar rutas con metodos GET POST PUT DELETE que le peguen al REPOSITORY ADAPTER
}